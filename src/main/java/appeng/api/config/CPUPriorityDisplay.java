package appeng.api.config;

public enum CPUPriorityDisplay {
    /// A badge in front of the CPU name.
    BADGE,
    /// A small number in the bottom right.
    COMPACT,
    /// Nothing, tooltip only.
    OFF
}
